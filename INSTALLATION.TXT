
Tested in: R version 3.3.1 (2016-06-21)

Download the packages from Github in to a forder, ex. DIR='src/'

Type following commands in R:
> install.packages("devtools")
> library(devtools)
> install(DIR)

Can skip the install.packages("devtools") if user has it already.
